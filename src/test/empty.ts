// Stub used by Vitest to neutralize the `server-only` import guard so that
// server-only modules with pure, testable logic (e.g. rate-limit) can be
// imported in the Node test environment.
export {};
